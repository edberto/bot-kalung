# Complete dependency set for the headless server worker (Linux) — the PC-free
# ingest + BNCT poll. Self-contained: install ONLY this, NOT requirements.txt
# (which carries the desktop GUI/Windows deps — PyQt6, xlwings, pyinstaller —
# that the server never uses).
#
#     pip install -r requirements-server.txt
openpyxl>=3.1                       # .xlsx workbook reader
xlrd>=2.0                           # .xls workbook reader
requests>=2.31                      # BNCT portal HTTP
google-api-python-client>=2.190     # Drive API scan
google-auth>=2.50                   # service-account auth
psycopg[binary]>=3.1                # Postgres (Supabase) backend
psycopg-pool>=3.2                    # connection pool (reuse warm conns per poll)
